// Expo discovers config plugins via this file at the package root.
// It re-exports the built plugin output so consumers don't need to point at
// `plugin/build/index.js` themselves.
module.exports = require('./plugin/build');
